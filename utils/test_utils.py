# test_utils.py

from data import KADID10KDataset
import numpy as np

def get_results(model, data_base_path, datasets, num_splits, phase, alpha, grid_search, crop_size, batch_size, num_workers, device):
    # 기존 get_results 함수 코드 작성
    pass
